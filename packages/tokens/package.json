{
    "name": "@mljar/mercury-tokens",
    "version": "0.1.0",
    "description": "Shared tokens and interfaces for Mercury extensions.",
    "license": "Apache-2.0",
    "main": "lib/index.js",
    "types": "lib/index.d.ts",
    "files": [
        "lib/**/*.{js,js.map,d.ts}"
    ],
    "scripts": {
        "build": "tsc -b",
        "build:prod": "tsc -b",
        "clean": "rimraf lib tsconfig.tsbuildinfo"
    },
    "dependencies": {
        "@jupyterlab/notebook": "^4.0.0",
        "@lumino/coreutils": "^2.0.0"
    },
    "devDependencies": {
        "rimraf": "^5.0.0",
        "typescript": "~5.0.4"
    }
}
